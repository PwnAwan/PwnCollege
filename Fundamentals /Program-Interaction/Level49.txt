Read the challenge file by typing /challenge/embryoio_level49
The challenge checks for a specific parent process : ipython
The challenge checks for a specific process at the other end of stdout : grep

Write the script as:

Run the script as "bash my_script.sh"  and then enter the password "icgtasos".
The flag will show.
